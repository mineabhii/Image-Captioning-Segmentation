# Image-Captioning-Segmentation
This project integrates image captioning (using CNN Encoder + LSTM Decoder) and image segmentation (using U-Net).  It uses the MS COCO 2017 dataset via the COCO API, ensuring memory-efficient loading without duplicating dataset files. A Flask web app provides an interactive UI to upload images and see both captions + segmentation results.
